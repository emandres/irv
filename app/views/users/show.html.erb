<p id="notice"><%= notice %></p>

<p>
  <b>Email:</b>
  <%= @user.email %>
</p>

<h2>Roles</h2>
<ul>
  <% @user.roles.each do |r| %>
    <li><%= r.name %>
  <% end %>
</ul>

<%= link_to 'Edit Roles', user_authorizations_path(@user) %>
<%= link_to 'Back', users_path %>
